server {
    root /var/www/html;
    listen 8005;

    location /api {
        include proxy_params;
        proxy_pass http://localhost:8000;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}